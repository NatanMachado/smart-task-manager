spring:
  application:
    name: SmartTaskManager
server:
  port: 8080

datasource:
  url: jdbc:h2:mem:tasksdb
  driverClassName: org.h2.Driver
  username: sa
  password: 
jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
